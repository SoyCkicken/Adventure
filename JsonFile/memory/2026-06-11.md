2026-06-11

- Workspace: C:\Users\pc\OneDrive\문서\Unity\Adventure\JsonFile
- Scope: P0/P1 stabilization follow-up plus Excel-to-JSON equipment data smoke test.

- Code/data changes:
  - JsonManager P0 compatibility hardening was kept in place:
    - validates JSON root keys before runtime parsing;
    - preserves the JSON key `One-Handed` while mapping it to the C# field `One_Handed`;
    - treats authoring/support JSON such as `ChoiceCondition(s)` as non-runtime data for now;
    - keeps item lookup based on `Item_ID`.
  - StoryDisplayManager P1 navigation cleanup was kept in place:
    - moved story choice target normalization/skip logic into `StoryNodeNavigator`.
  - GameScene missing script cleanup was kept in place.
  - Added EditMode compatibility tests under `Assets/Tests/EditMode`.
  - Added test weapon data through `Assets/ExcelFiles/AllItem_Master.xlsx`:
    - `Weapon_ID`: `Weapon_013`
    - `Weapon_Name`: `Option_003`
    - `Option_1_ID`: `Option_003`
    - `Option_Value1`: `5`
    - `One-Handed`: `true`
    - note: `Weapon_Name` intentionally uses existing ASCII sprite key `Option_003` because the current Excel conversion path corrupts Korean names and the inventory slot uses item name as the sprite lookup key.
  - Re-ran the existing Unity editor converter logic from `Tools/Excel Auto Generator`; generated `Assets/Resources/Events/Weapon_Master.json` now contains 13 weapons.

- Verification:
  - Unity script validation:
    - `Assets/Json/Weapon_Master.cs`: 0 diagnostics.
    - `Assets/Tests/EditMode/P0DataCompatibilityTests.cs`: 0 diagnostics.
  - EditMode tests:
    - total 17, passed 17, failed 0.
  - Runtime smoke through Unity MCP:
    - started from `LobbyScenes`, confirmed `JsonManager` loaded 13 weapons, then loaded `GameScene`;
    - added `Weapon_013` to inventory list;
    - equipped via `EquipmentSystem.EquipItem`;
    - confirmed `player.weapon_Name == Weapon_013`;
    - confirmed `Option_003` OnHit option registered once with value 5;
    - applied burn smoke target, HP changed from 100 to 98;
    - unequipped via `EquipmentSystem.UnequipItem`;
    - confirmed remaining `Weapon_013` OnHit options became 0;
    - final Unity console Error/Warning count: 0.

- Excluded/side-effect files:
  - Do not include Unity-generated zip side effects:
    - `Assets/Editor.zip`
    - `Assets/ExcelFiles.zip`
    - `Assets/Script.zip`
    - matching `.meta` files.
  - Treat `ProjectSettings/EditorSettings.asset` as a Unity test/play-mode side effect unless explicitly needed.

- Operational notes:
  - GameScene direct play does not create `JsonManager`; use the normal `LobbyScenes -> GameScene` flow for runtime smoke tests.
  - Unity MCP may intermittently log `Cannot access a disposed object` for its client stream during refresh/play transitions. In this run it did not correspond to a game error and final console was clean.

(End of 2026-06-11 entries)
