2026-06-11

- Workspace: C:\Users\pc\OneDrive\문서\Unity\Adventure\JsonFile
- Scope: P0/P1 stabilization follow-up plus Excel-to-JSON equipment data smoke test.

- Code/data changes:
  - JsonManager P0 compatibility hardening was kept in place:
    - validates JSON root keys before runtime parsing;
    - preserves the JSON key `One-Handed` while mapping it to the C# field `One_Handed`;
    - treats authoring/support JSON such as `ChoiceCondition(s)` as non-runtime data for now;
    - keeps item lookup based on `Item_ID`.
  - StoryDisplayManager P1 navigation cleanup was kept in place:
    - moved story choice target normalization/skip logic into `StoryNodeNavigator`.
  - GameScene missing script cleanup was kept in place.
  - Added EditMode compatibility tests under `Assets/Tests/EditMode`.
  - Added test weapon data through `Assets/ExcelFiles/AllItem_Master.xlsx`:
    - `Weapon_ID`: `Weapon_013`
    - `Weapon_Name`: `Option_003`
    - `Option_1_ID`: `Option_003`
    - `Option_Value1`: `5`
    - `One-Handed`: `true`
    - note: `Weapon_Name` intentionally uses existing ASCII sprite key `Option_003` because the current Excel conversion path corrupts Korean names and the inventory slot uses item name as the sprite lookup key.
  - Re-ran the existing Unity editor converter logic from `Tools/Excel Auto Generator`; generated `Assets/Resources/Events/Weapon_Master.json` now contains 13 weapons.

- Verification:
  - Unity script validation:
    - `Assets/Json/Weapon_Master.cs`: 0 diagnostics.
    - `Assets/Tests/EditMode/P0DataCompatibilityTests.cs`: 0 diagnostics.
  - EditMode tests:
    - total 17, passed 17, failed 0.
  - Runtime smoke through Unity MCP:
    - started from `LobbyScenes`, confirmed `JsonManager` loaded 13 weapons, then loaded `GameScene`;
    - added `Weapon_013` to inventory list;
    - equipped via `EquipmentSystem.EquipItem`;
    - confirmed `player.weapon_Name == Weapon_013`;
    - confirmed `Option_003` OnHit option registered once with value 5;
    - applied burn smoke target, HP changed from 100 to 98;
    - unequipped via `EquipmentSystem.UnequipItem`;
    - confirmed remaining `Weapon_013` OnHit options became 0;
    - final Unity console Error/Warning count: 0.

- Excluded/side-effect files:
  - Do not include Unity-generated zip side effects:
    - `Assets/Editor.zip`
    - `Assets/ExcelFiles.zip`
    - `Assets/Script.zip`
    - matching `.meta` files.
  - Treat `ProjectSettings/EditorSettings.asset` as a Unity test/play-mode side effect unless explicitly needed.

- Operational notes:
  - GameScene direct play does not create `JsonManager`; use the normal `LobbyScenes -> GameScene` flow for runtime smoke tests.
  - Unity MCP may intermittently log `Cannot access a disposed object` for its client stream during refresh/play transitions. In this run it did not correspond to a game error and final console was clean.

(End of 2026-06-11 entries)

2026-06-11 P2 foundation update

- Scope:
  - Started P2 with the safer sequence agreed in chat:
    1. add narrow `JsonManager` ID lookup APIs;
    2. centralize `ItemData` construction;
    3. keep Story/Event merge deferred, with existing helper boundaries preserved.

- Code changes:
  - Added `Assets/Script/GamePlay/ItemDataFactory.cs`.
    - Converts `Weapon_Master`, `Armor_Master`, `Item_Master`, and `MerchantItem` into `ItemData`.
    - Normalizes `"null"` option IDs to null.
    - Provides shared option/stat helpers for UI code.
  - Updated `JsonManager`.
    - Added `GetWeaponById`, `GetArmorById`, `GetItemMasterById`, and `GetOptionById`.
    - Builds ID lookup indexes after JSON load.
    - Keeps existing list getters for compatibility.
    - Keeps `GetItemDataFromCode` and `FindItemDataByCode`, but routes construction through `ItemDataFactory`.
  - Updated item-related callers.
    - `InventoryManager`: item detail, stat text, option text, and consumable use now rely on master-enriched `ItemData`.
    - `EquipmentSystem`: equipment lookup uses ID API and skips `"null"` options.
    - `MerchantManager`: purchase/detail conversion uses `ItemDataFactory`; buy button listener is cleared before adding a new listener.
    - `RemoteTester`: debug item add uses `ItemDataFactory.FromCode`.
    - `PendingItemUI`: equipment details use ID lookup APIs and safer option checks.

- Verification:
  - Unity script validation:
    - `ItemDataFactory.cs`, `JsonManager.cs`, `InventoryManager.cs`, `EquipmentSystem.cs`, `MerchantManager.cs`, `RemoteTester.cs`, `PendingItemUI.cs`, and `P0DataCompatibilityTests.cs`: 0 diagnostics.
  - EditMode tests:
    - total 18, passed 18, failed 0.
  - Runtime smoke through Unity MCP:
    - started from `LobbyScenes`, confirmed `GetWeaponById("Weapon_013")` and `GetOptionById("Option_003")`;
    - loaded `GameScene`;
    - created `Weapon_013` through `ItemDataFactory.FromCode`;
    - equipped via `EquipmentSystem.EquipItem`;
    - confirmed `Option_003` OnHit option registered once;
    - applied burn target, HP changed from 100 to 98;
    - unequipped and confirmed remaining `Weapon_013` OnHit options became 0.
  - Final console after clearing MCP bridge noise:
    - Error 0, Warning 0.

- Notes:
  - `RemoteTester.cs` and `PendingItemUI.cs` are not UTF-8 encoded, so edits were made while preserving Windows default encoding.
  - Story/Event code was not merged in this pass. Current direction is to extract small shared helpers first, then merge only after stable coverage exists.

2026-06-12 P2 equipment effect update

- Scope:
  - Continued P2 in the requested order:
    1. Excel-to-JSON conversion hardening;
    2. equipment effect expansion;
    3. equipment/effect test coverage.

- Code/data changes:
  - Updated `Assets/Editor/ExcelToJsonConverter.cs`.
    - Uses explicit Excel fallback encoding for Korean legacy workbooks when available.
    - Writes generated JSON and C# files with explicit UTF-8 output.
  - Updated `Assets/ExcelFiles/AllItem_Master.xlsx` and regenerated JSON/classes through the Unity converter.
    - `Weapon_013` now uses the Korean name `화염 검` instead of the previous ASCII sprite-key workaround.
    - `Option_008` is now `Effect_008`, `OnHit`, description `적중 시 대상의 다음 공격 1회 실패`.
  - Added `ForceNextAttackMissEffect`.
    - On hit, adds a one-shot forced-miss charge to the target.
    - `Character.Attack` consumes the charge before damage/crit calculation and returns 0 damage.
  - Updated icon lookup to try `Icon`, `Item_ID`, `Item_Name`, then default slot fallback.
  - Preserved the item source ID in `CombatTest` OnHit contexts.

- Verification:
  - Unity script validation for changed converter/combat/UI/test scripts: 0 diagnostics.
  - EditMode tests:
    - total 23, passed 23, failed 0.
  - Runtime smoke through Unity MCP:
    - `ForceNextAttackMissEffect` applied to target;
    - first target attack returned 0 damage and victim HP stayed 100;
    - second target attack returned 10 damage and victim HP became 90.
  - Final Unity console Error/Warning count: 0.

2026-06-12 P2 periodic effect value update

- Code changes:
  - Updated `Character` periodic burn/heal calculation.
    - `Option_003` and `Option_004` now use `BuffData.Value` as a percent of the target max health.
    - Example: `Value = 10` applies 10% immediate burn/heal and 10% again on each tick.

- Verification:
  - Unity script validation:
    - `Character.cs` and `P0DataCompatibilityTests.cs`: 0 diagnostics.
  - EditMode tests:
    - total 23, passed 23, failed 0.
  - Runtime smoke through Unity MCP:
    - burn `Value=10`: HP `100 -> 90 -> 80`;
    - heal `Value=10`: HP `70 -> 80 -> 90`.
  - Final Unity console Error/Warning count: 0.

2026-06-12 P2 stackable status effect expansion

- Scope:
  - Added the first generalized stack/refresh status-effect runtime layer while preserving current combat flow.

- Code/data changes:
  - Extended `Option_Master` runtime class and JSON with status metadata:
    - `StatusType`, `ApplyMode`, `StackPolicy`, `MaxStack`, `Duration`, `TickInterval`, `TriggerType`,
      `ValueMode`, `BaseChance`, `ChancePerStack`, `BaseValue`, `ValuePerStack`, `StatType`,
      `ResistanceType`, `MaxRemoveCount`.
  - Added new options in runtime JSON:
    - `Option_009` Bleed, `Option_010` Poison, `Option_011` Holy, `Option_012` Regen, `Option_013` Freeze.
  - Extended `Character`.
    - Supports stackable status keys by `StatusType`.
    - Handles `BeforeAttack` action blocking / miss chance.
    - Handles `OnAttack` bleed/poison self-damage, regen self-heal, holy next-attack miss chance.
    - Handles freeze speed reduction and freeze action block chance.
    - Added simple resistance fields for debuff damage, bleed, poison, and freeze.
  - Extended `OptionManager`.
    - Added stack-effect appliers for Bleed, Poison, Holy, Regen, and Freeze.

- Verification:
  - Unity script validation:
    - `Character.cs`, `OptionManager.cs`, `Option_Master.cs`, and `P0DataCompatibilityTests.cs`: 0 diagnostics.
  - EditMode tests:
    - total 27, passed 27, failed 0.
  - Runtime smoke through Unity MCP:
    - bleed stack smoke: HP `100 -> 96`;
    - regen stack smoke: HP `50 -> 52`;
    - freeze stack smoke: speed `10 -> 8.5`, attack damage `0`;
    - holy stack smoke: target next attack damage `0`, victim HP stayed `100`.
  - Final Unity console Error/Warning count: 0.

- Open issue:
  - `Assets/ExcelFiles/AllItem_Master.xlsx` is currently locked by an external process (`~$AllItem_Master.xlsx` exists),
    so the expanded option metadata could not be written back into the workbook in this pass.
    Runtime JSON and generated class were updated directly; update the workbook after closing Excel.

2026-06-12 P2 Excel option metadata persisted

- Follow-up:
  - After the workbook lock was released, wrote the expanded status-effect metadata columns and `Option_009` through `Option_013`
    back into `Assets/ExcelFiles/AllItem_Master.xlsx`.
  - Re-ran the Unity Excel converter for all 6 Excel files.
  - Confirmed the workbook and generated `Option_Master.json` both preserve Korean descriptions for Bleed/Holy/Freeze.

- Verification:
  - Unity script validation:
    - `OptionManager.cs`, `Character.cs`, `Option_Master.cs`, and `P0DataCompatibilityTests.cs`: 0 diagnostics.
  - EditMode tests:
    - total 27, passed 27, failed 0.
  - Runtime smoke through Unity MCP:
    - bleed HP `100 -> 96`;
    - regen HP `50 -> 52`;
    - freeze speed `10 -> 8.5`, freeze attack damage `0`.
  - Final Unity console Error/Warning count after clearing MCP bridge noise: 0.

2026-06-12 P2 option authoring split and equipment-route verification

- Scope:
  - Ignored balance tuning and focused on authoring structure, real stat/resistance connection, and actual equipment-route tests.

- Code/data changes:
  - Split Excel authoring data:
    - `Option_Master` now keeps the core option identity columns.
    - New `OptionEffect_Master` keeps effect/status metadata.
    - `ExcelAutoGenerator` merges `OptionEffect_Master` metadata back into generated `Option_Master.json` and `Option_Master.cs`, preserving the existing runtime contract.
  - Added resistance options:
    - `Option_014` DebuffDamageResist, `Option_015` BleedResist, `Option_016` PoisonResist, `Option_017` FreezeResist.
  - Added `Armor_011` as a test armor with `Option_014` and `Option_015`.
  - Added `StatBonusEffect` and connected `ApplyMode=Stat` / `StatType` to `Character` fields.
  - `JsonManager` now treats `OptionEffect_Master` as authoring/merge data so it does not emit unknown-file warnings at runtime.

- Verification:
  - Unity script validation:
    - `ExcelToJsonConverter.cs`, `JsonManager.cs`, `OptionManager.cs`, `Character.cs`, `OptionEffect_Master.cs`,
      and `P0DataCompatibilityTests.cs`: 0 diagnostics.
  - EditMode tests:
    - total 31, passed 31, failed 0.
    - Added equipment-route tests for `Armor_011` resistance stats and `Weapon_013` on-hit burn registration/effect firing.
  - PlayMode test path:
    - runner completed successfully; current project still has 0 concrete PlayMode test cases.
  - Final Unity console Error/Warning count after clearing MCP bridge noise: 0.
